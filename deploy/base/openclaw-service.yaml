apiVersion: v1
kind: Service
metadata:
  name: openclaw
  namespace: openclaw-gateway
  labels:
    app: openclaw
spec:
  selector:
    app: openclaw
  ports:
    - name: gateway
      port: 18789
      targetPort: gateway
      protocol: TCP
